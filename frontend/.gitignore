# Created by Gemini for a React + Vite project

# Dependencies
# Ignore the node_modules directory to avoid committing thousands of files.
/node_modules

# Build artifacts
# Ignore the production build folder. Vite uses 'dist' by default.
/dist
/build

# Log files
# Ignore log files generated by npm, yarn, etc.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite-specific cache and files
# Vite's dependency cache directory
.vite
# Optional: coverage reports from testing
/coverage

# Environment Variables
# Ignore all local environment variable files.
# These files often contain sensitive API keys and should NEVER be committed.
.env
.env.local
.env.*.local

# It's a good practice to commit a template file for environment variables.
# The '!' negates the ignore rule for this specific file.
!/.env.example

# IDE / Editor specific files
# VS Code
.vscode/

# Operating System specific files
# macOS
.DS_Store

# Windows thumbnail cache
Thumbs.db